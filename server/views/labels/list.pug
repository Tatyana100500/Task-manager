extends ../layouts/application.pug

block content
  a.nav-link(href=route('newLabel'))= t('views.labels.list.create')
  table.table.table-hover
    thead
      tr
        th(scope='col')= t('views.labels.list.id')
        th(scope='col')= t('views.labels.list.name')
        th(scope='col')= t('views.labels.list.createdAt')
        th
    tbody
      each label in labels
        tr
          td= label.id
          td= label.name
          td= formatDate(label.createdAt)
          td
            a(href=`/labels/${label.id}/edit`)= t('views.labels.list.edit')
            +buttonTo(`/labels/${label.id}`, 'delete')(value=t('views.labels.list.delete') class="btn btn-link")